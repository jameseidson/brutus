load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "ffi",
    hdrs = ["ffi.h"],
    visibility = ["//visibility:public"],
    deps = [
        "//src/client",
        "//src/server",
    ],
)
